spring:
  application:
    name: com.integrador1

  datasource:
    url: jdbc:postgresql://localhost:5432/siga_db
    username: jkevin
    password: Kevin_324
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  error:
    include-binding-errors: always
    include-message: always
    
logging:
  file:
    name: logs/gestrack-app.log
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 30
  pattern:
    file: "%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %5p %-40.40logger{39} : [Usuario: %X{username}] - %m%n"
  level:
    root: WARN
    com.integrador1: INFO